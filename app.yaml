runtime: nodejs14
entrypoint: ts-node --project ./tsconfig-server.json -r esm -r tsconfig-paths/register ./src/server.ts
instance_class: F1

automatic_scaling:
  max_instances: 1
  min_instances: 0

env_variables:
  NODE_ENV: $NODE_ENV
  SENTRY_DSN: $SENTRY_DSN
  ENVIRONMENT: $ENVIRONMENT
